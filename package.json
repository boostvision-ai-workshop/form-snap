{
  "name": "micro-saas-agent",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --dir frontend dev\" \"cd backend && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\"",
    "test": "pnpm --dir frontend test --run && uv run --project backend pytest -q",
    "build": "pnpm --dir frontend build",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down"
  },
  "pnpm": {
    "patchedDependencies": {
      "next-themes@0.4.6": "patches/next-themes@0.4.6.patch"
    }
  }
}
